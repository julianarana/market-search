import React, { ReactElement } from 'react';
import search from '../../../assets/images/ic_Search.png';

const Search = (): ReactElement => {
  return <img style={{ width: '18px', height: '20px' }} src={search} />;
};

export default Search;
